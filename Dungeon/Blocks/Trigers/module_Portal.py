from module_links import levels, intoxicated, game

class Portal(object):

	des = '@'

	def __init__(self):

		super().__init__()

	def act(self, obj):
		#, obj):
#		if len(levels) > 1:
#			obj.area = levels[levels.index(obj.area) + 1]
#			obj.area.generator()
#			
#			location = obj.location
#			location['row'], location['elm'] = obj._fst_row, obj._fst_elm
#			obj.hlt = obj.full_hlt
#			obj.mana   = obj.full_mana
#			if obj in intoxicated:
#				del intoxicated[obj]
#		else:
		pass